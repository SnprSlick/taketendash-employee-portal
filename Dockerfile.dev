FROM node:20-alpine

WORKDIR /app

# Install dependencies separately for better layer caching
COPY package*.json ./
RUN npm ci

# Source files are mounted as a volume in dev
COPY . .

EXPOSE 3002

CMD ["npm", "run", "dev", "--", "-p", "3002"]
